"""
app/models/__init__.py

This file marks the `models` directory as a Python package,
which allows other parts of the app to import from it like:

    from app.models.business_profile import BusinessProfile

No code is needed here for now. As the project grows, this file
could be used to expose a clean public API for the models package
(re-exporting the classes you want available at the package level).
"""
